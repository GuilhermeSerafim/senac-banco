SELECT       -- o que você quer ver
FROM         -- de qual tabela
WHERE        -- filtros linha a linha (antes do agrupamento)
GROUP BY     -- agrupamento (ex: por cliente, por produto...)
HAVING       -- filtro dos grupos (após agrupamento)
ORDER BY     -- ordenação do resultado final
LIMIT        -- limita a quantidade de linhas (opcional)